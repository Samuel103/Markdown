interface:
  display_name: "feature"
  short_description: "Capture a feature and iterate its plan"
  default_prompt: "Use $feature with this feature Markdown file to capture it and prepare an implementation plan."
policy:
  allow_implicit_invocation: false
