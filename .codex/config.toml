[agents]
enabled = true
max_concurrent_threads_per_session = 4

[agents.reuse-scout]
description = "Read-only React/TypeScript scout for reusable code; use before creating a shared abstraction."

config_file = "agents/reuse-scout.toml"
